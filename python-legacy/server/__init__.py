"""Web server package for ATC_Transcribe.

Serves a browser UI that talks to the Apple-Silicon (or any) host running the
fine-tuned Whisper transcriber: live ATC transcripts, a stream-URL control bar,
and a handshake / proof-of-life status panel.
"""
