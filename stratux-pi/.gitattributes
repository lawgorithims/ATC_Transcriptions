# These run on the Raspberry Pi (Linux) — force LF so a Windows checkout can't write CRLF and break
# the shebang / shell parsing.
*.sh         text eol=lf
*.py         text eol=lf
*.service    text eol=lf
*.env        text eol=lf
Makefile     text eol=lf
bin/*        text eol=lf
